{
  "id": "create-channel-label-spec",
  "action_name": "createChannelLabelSpec",
  "name": "Add a label spec",
  "icon": "emoji:➕",
  "title": "Set up labels for a channel",
  "help_text": "Takealot: EAN-13. Amazon FBA: Code 128 (the FNSKU). The label stock is used for every print run on this channel — the flow never asks. Once the spec exists, add its label elements from the spec page: a channel with none prints the product name over the barcode and nothing else.",
  "inputs": {
    "$.channel.id": { "display_name": "Channel", "display_order": 1 },
    "$.symbology": { "display_name": "Symbology", "display_order": 2 },
    "$.defaultStock": { "display_name": "Label stock", "display_order": 3 }
  },
  "response": {
    "$.id": {
      "link": {
        "tool_id": "get-channel-label-spec",
        "display_order": 0,
        "data_mapping": [{ "key": "$.id", "path": { "path": "$.id" } }]
      }
    }
  }
}
