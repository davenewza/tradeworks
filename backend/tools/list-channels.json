{
  "id": "list-channels",
  "action_name": "listChannels",
  "name": "Channels",
  "icon": "emoji:📺",
  "response": {
    "$.results[*].createdAt": {
      "visible": false
    },
    "$.results[*].id": {
      "link": {
        "tool_id": "get-channel",
        "display_order": 0,
        "data_mapping": [
          {
            "key": "$.id",
            "path": {
              "path": "$.results[*].id"
            }
          }
        ]
      }
    },
    "$.results[*].updatedAt": {
      "visible": false
    }
  },
  "display_layout": {
    "config": {
      "gallery_config": {
        "title": {
          "template": "{{$.results[*].name}}"
        }
      },
      "type": 5
    }
  }
}